# Fabric compatibility tester

This project builds and runs client gametests against a prebuilt `MouseTweaks.jar` file.
Intended use: automatically test Mouse Tweaks against a new Minecraft patch release.

- Download and put `MouseTweaks.jar` into `fabric-compat-test/`
- Run `./gradlew -p fabric-compat-test prodClient`
